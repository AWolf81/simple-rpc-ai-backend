/**
 * Task management procedures for MCP
 *
 * Note: All task management tools (longRunningTask, cancellableTask, cancelTask,
 * listRunningTasks, getTaskProgress) have been moved to the 04-mcp-tasks-server
 * example to keep core clean and focused on essential functionality.
 */
export const taskProcedures: Record<string, any> = {
  // Essential task procedures can be added here
  // Currently empty as task management tools have been moved to examples
};