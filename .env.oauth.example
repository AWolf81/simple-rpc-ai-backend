# Google OAuth2 Configuration for MCP Authentication
# Copy this file to .env.oauth and fill in your Google OAuth2 credentials

# Get these from: https://console.developers.google.com
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Go to Credentials > Create Credentials > OAuth 2.0 Client ID
# 4. Application type: Web application
# 5. Authorized redirect URIs: http://localhost:8082/oauth/callback

GOOGLE_CLIENT_ID=your_google_client_id_here.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Token encryption key (32 hex characters = 16 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If not provided, a random key will be generated each server restart (tokens won't persist)
TOKEN_ENCRYPTION_KEY=your_32_character_hex_encryption_key_here

# Optional: Customize the server port (default: 8082)
# SERVER_PORT=8082

# Note: Make sure to add http://localhost:8082/oauth/callback 
# as an authorized redirect URI in your Google OAuth2 app settings